# Access code visitors must enter once. Leave empty to make the site open to anyone with the link.
API_KEY=
# Largest video the server will download, in MB.
MAX_FILESIZE_MB=500
# Only for "docker compose --profile named": token from the Cloudflare dashboard
# (Zero Trust > Networks > Tunnels > your tunnel).
TUNNEL_TOKEN=
