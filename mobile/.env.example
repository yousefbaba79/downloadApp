# URL of the download server (see ../server). On a physical phone use your computer's LAN IP.
EXPO_PUBLIC_API_URL=http://192.168.1.20:8000
# Only needed if the server was started with API_KEY set.
EXPO_PUBLIC_API_KEY=
